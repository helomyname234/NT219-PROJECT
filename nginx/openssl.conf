openssl_conf = default_conf

[default_conf]
ssl_conf = ssl_sect

[ssl_sect]
system_default = system_default_sect

[system_default_sect]
# Hạ mức bảo mật từ 2 (mặc định) xuống 1 để chấp nhận khóa Dilithium
CipherString = DEFAULT@SECLEVEL=1
